Spring:
  datasource:
#    url: jdbc:h2:tcp://localhost/~/jpashop
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100 # SQL 를 생성할 때, in 절에 대한 사이즈.
#  h2:
#    console:
#      enabled: true
logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace